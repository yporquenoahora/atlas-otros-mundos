{
  "name": "atlas-otros-mundos",
  "version": "0.1.0",
  "type": "module",
  "description": "Atlas interactivo de Otros Mundos — Thomas Halliday",
  "scripts": {
    "dev": "vite dev --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy:gh": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "gh-pages": "^6.1.1",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  }
}